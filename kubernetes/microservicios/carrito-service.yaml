apiVersion: v1
kind: Service
metadata:
  name: carrito
  labels:
    app: carrito
spec:
  selector:
    app: carrito-microservicio
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP